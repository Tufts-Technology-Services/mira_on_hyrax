name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      NOKOGIRI_USE_SYSTEM_LIBRARIES: true
      DEFAULT_ADMIN_SET: 'default'
      DATABASE_AUTH: true
      DATA_STORAGE: ./public
      TEMP_STORAGE: ./public
      ALLOW_NOTIFICATIONS: true
      DB_DATABASE: epigaea_test
      DB_USER: root
      DB_PASSWORD: root
      TRAVIS: true
      SOLR_URL: http://127.0.0.1:8985/solr/test
      FEDORA_URL: http://127.0.0.1:8986/rest

    services:
      redis:
        image: redis
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    strategy:
      fail-fast: false
      matrix:
        ruby-version: ['2.7.7']
    steps:
    - name: Pin chrome
      uses: abhi1693/setup-browser@v0.3.4
      with:
        browser: chrome
        version: 1036826
    - name: Install OS packages
      run: |
        sudo apt-get update
        sudo apt-get install unzip imagemagick libmagickwand-dev ghostscript libpq-dev libxslt-dev
    - uses: actions/checkout@v2
    - name: Set up Ruby
    # To automatically get bug fixes and new Ruby versions for ruby/setup-ruby,
    # change this to (see https://github.com/ruby/setup-ruby#versioning):
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: ${{ matrix.ruby-version }}
        bundler: "2.2.26"
    # TODO: Tempoary remove this update untill ruby 3
    # - name: Update rubygems
    #   run: |
    #     gem install bundler:2.1.4
    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: 11

    - name: Install FITS
      run: |
        FITS_VERSION=1.5.0
        curl --fail --location --retry 3 --output "$RUNNER_TEMP/fits-${FITS_VERSION}.zip" "https://github.com/harvard-lts/fits/releases/download/${FITS_VERSION}/fits-${FITS_VERSION}.zip"
        sudo mkdir -p /opt/fits
        sudo unzip -q "$RUNNER_TEMP/fits-${FITS_VERSION}.zip" -d /opt/fits
        sudo sed -i '/edu\.harvard\.hul\.ois\.fits\.tools\.mediainfo\.MediaInfo/d' /opt/fits/xml/fits.xml
        sudo chmod u+x /opt/fits/fits.sh
        /opt/fits/fits.sh -v
        if ! /opt/fits/fits.sh -i spec/fixtures/files/pdf-sample.pdf > "$RUNNER_TEMP/fits-smoke.xml" 2> "$RUNNER_TEMP/fits-smoke.err"; then
          cat "$RUNNER_TEMP/fits-smoke.err"
          exit 1
        fi
    - name: Set up MySQL
      run: |
        sudo systemctl enable mysql.service
        sudo systemctl start mysql.service
        mysql -e 'CREATE DATABASE ${{ env.DB_DATABASE }};' -u${{ env.DB_USER }} -p${{ env.DB_PASSWORD }}
    - name: Configure test environment
      run: |
        cp config/fedora.yml.sample config/fedora.yml
        cp config/solr.yml.sample config/solr.yml
        cp config/devise.yml.sample config/devise.yml
        cp config/redis.yml.sample config/redis.yml
        cp config/database.yml.sample config/database.yml
        cp config/blacklight.yml.sample config/blacklight.yml
        cp config/handle.yml.sample config/handle.yml
        cp config/secrets.yml.sample config/secrets.yml
    - name: Create database
      run: |
        RAILS_ENV=test bundle exec rake db:create
        RAILS_ENV=test bundle exec rake db:migrate --trace
    - name: Start test containers
      run: |
        docker run -d --name ci-solr-test \
          -p 8985:8983 \
          -v "$GITHUB_WORKSPACE/solr/conf:/opt/solr/test-core-config/conf" \
          solr:6.6.1 \
          solr-precreate test /opt/solr/test-core-config

        docker run -d --name ci-fcrepo-test \
          -p 8986:8080 \
          ghcr.io/samvera/fcrepo4:4.7.5

        for i in {1..90}; do
          if curl --silent --fail "$SOLR_URL/admin/ping" > /dev/null && \
             curl --silent --fail "$FEDORA_URL" > /dev/null; then
            exit 0
          fi
          sleep 5
        done

        docker logs ci-solr-test
        docker logs ci-fcrepo-test
        exit 1
    - name: Set up sipity
      run: |
        RAILS_ENV=test bundle exec rake hyrax:workflow:load
    - name: Rubocop
      run: |
        bundle exec rubocop
    - name: Compile assets
      run: |
        bundle exec rails assets:precompile &> /dev/null
    - name: Run tests
      run: xvfb-run -a bundle exec rake spec
      env:
        RAILS_ENV: test
        TMPDIR: /tmp
    - name: Coveralls
      uses: coverallsapp/github-action@master
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
